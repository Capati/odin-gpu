# OBJ Model

This example demonstrates how to load a `OBJ` model using tobj (tinyobjloader).

## Features

- Use the `tobj` utility to load the model.

## Running

To build and run this example:

```shell
build.bat|sh obj_model run
```

## Screenshots

![OBJ Model](./obj_model.png)
